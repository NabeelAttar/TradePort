{
  "name": "@trade-port/api-gateway",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "serve": {
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@trade-port/api-gateway:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@trade-port/api-gateway:build:development"
          },
          "production": {
            "buildTarget": "@trade-port/api-gateway:build:production"
          }
        }
      }
    }
  }
}
