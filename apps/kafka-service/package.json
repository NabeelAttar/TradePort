{
  "name": "@trade-port/kafka-service",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "serve": {
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@trade-port/kafka-service:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@trade-port/kafka-service:build:development"
          },
          "production": {
            "buildTarget": "@trade-port/kafka-service:build:production"
          }
        }
      }
    }
  }
}
